- content_for :title do
  = t '.title'

%ul#lists
  - @lists.each do |list|
    %li
      %span.counter= list.items.size
      = link_to list.name, list, :class => cycle(:even, :odd)
  %li
    %a#add{ :href => new_list_path }= t '.new'
